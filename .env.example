# Public canonical URL used for metadata and social cards.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Server-only credential for future GPT-5.6 routes. Never expose this as NEXT_PUBLIC_*.
OPENAI_API_KEY=
